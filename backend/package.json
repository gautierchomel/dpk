{
  "name": "dpk-apostrophe-backend",
  "version": "1.0.0",
  "private": true,
  "description": "ApostropheCMS backend for DPK",
  "scripts": {
    "dev": "node -r dotenv/config app @apostrophecms/asset:watch dotenv_config_path=.env",
    "start": "node -r dotenv/config app dotenv_config_path=.env"
  },
  "dependencies": {
    "apostrophe": "^4.29.0",
    "dotenv": "^16.6.1"
  }
}
